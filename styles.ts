import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        padding: 20,
    },
    listContainer: {
        padding: 3,
    },
    fileItem: {
        marginVertical: 10,
        padding: 10,
        backgroundColor: '#8B4513',
        borderRadius: 5,
    },
    fileText: {
        color: 'white',
    },
});